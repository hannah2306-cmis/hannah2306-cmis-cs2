def output(minimumNumber, maximumNumber, targetNumber, guess):
	out = """
What is the minumum number? {}
What is the maximum number? {}
I'm thinkng of a number from {} to {}.
""".format(minimumNumber, maximumNumber)
    return out

def output(minimumNumber, maximumNumber)
    out = """
I'm thinkng of a number from {} to {}.
""".format(minmumNumber, maximumNumber) 

def guessing = raw_input("What do you think it is?: ")

import random 

def randomize(minimum, maximum)  
    return random.randint(minimum, maximum)

def output(targetNumber, guess):
    out = """
The target was {}.
Your guess was {}.
That's under by {}.
""".format(
 
    print "I'm thinking of a number from 1-10." 
	targetNumber = raw_input("What do you think it is?: ")
    out = output(minimumNumber, maximumNumber, targetNumber) 
    print out


def main():
    minimumNumber = raw_input("What is the minimum number?: ") 
	maximumNumber = raw_input("What is the maximum number?: ")
    targetNumber = randomize(minimumNumber, maximumNumber) 
    output = """ 
I'm think of a number from {} to {}.
""".format(minimumNumber, maximumNumber)
    print output
    guessing = raw_input("What do you think it is?: ")   
    if targetNumber == guessing: 
        print """
The target was {}.
Your guess was {}.
That's right! You're a psychic!
""".format(targetNumber, guessing) 
    elif targetNumber < guessing: 
    difference = guessing - targetNumber 
    



    out = output(int(minimumNumber), int(maximumNumber), int(output), int(guess))
    print out 
main() 

def differenceOfguessingandtargetNumber(g, t)
    return 
difference = differenceofguessingandtargetnumber(guess, target) 



